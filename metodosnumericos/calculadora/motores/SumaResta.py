import numpy as np
import array as arr

def suma(matrixUno, matrixDos):
    mUno = np.array(matrixUno)
    mDos = np.array(matrixDos)
    return mUno + mDos

def resta(matrixUno, matrixDos):
    mUno = np.array(matrixUno)
    mDos = np.array(matrixDos)
    return mUno - mDos

